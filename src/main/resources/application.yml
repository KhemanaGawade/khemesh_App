server:
  port: 9091

spring:
  application:
    name: WELCOME-API-1
 
  boot:
    admin:
      client:
        url: http://localhost:1111/
        
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      
management:
  endpoints:
    web:
      exposure:
        exclude: '*'
        
        